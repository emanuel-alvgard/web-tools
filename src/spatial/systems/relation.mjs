let data;

let target = []; // -1 if face has no target
let prev_pos = [];
let multiplier = [];

function create(spatial_data) {
    // sets data to spatial data
}

function add(subject, subject_axis, target, target_axis, multiplier) {

    lock_target[subject] = target;
    lock_multiplier[subject] = multiplier;

}

function remove(subject) {}
function update()