# a = String

# b = Uint8
# c = Uint16
# d = Uint32
# e = Uint64
# f = Uint128
# g = Uint256
# h = Uint512
# i = Uint1024

# j = Int8
# k = Int16
# l = Int32
# m = Int64
# n = Int128
# o = Int256
# p = Int512
# q = Int1024

# r = Float32
# s = Float64
# t = Float128
# u = Float256
# v = Float512
# w = Float1024

# each '|' denotes a index
|a"hello"*100|b1,2,3,4|a"world"/1/34/50"last"*10|r1.2345/1/20/25,3.564*18

save(target_array, ltf_string); # can only append to the end of the ltf-string.
# grammar: save target_array into ltf_string.

index(ltf_string, index_array); # indexes an ltf-sting into a index-array for fast access.
# grammar: index ltf_string into index_array.

load(0, ltf_string, index_array) # can load from a specified index of the ltf-string. Last paramter is optional and can be set to null.
# grammar: load array[0] from ltf_string using index_array.







